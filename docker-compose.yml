services:
  web:
    build: .
    ports:
      - "${HOST_PORT:-8081}:${PORT:-8080}"
    environment:
      PORT: ${PORT:-8080}
      BACKEND_UPSTREAM: ${BACKEND_UPSTREAM:-http://host.docker.internal:3000}
      API_BASE_URL: ${API_BASE_URL:-/api}
    restart: unless-stopped
